# ⚡ Быстрое развертывание Food2Mood API

Краткая инструкция для быстрого запуска API в продакшене.

## 🚀 Вариант 1: Render (Самый простой, бесплатный тариф)

1. Зарегистрируйтесь на https://render.com
2. Подключите GitHub репозиторий
3. Создайте новый **Web Service**
4. Настройки:
   - **Name**: `food2mood-api`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r api/requirements.txt`
   - **Start Command**: `uvicorn api.main_prod:app --host 0.0.0.0 --port $PORT`
5. Добавьте переменные окружения:
   - `DATABASE_PATH`: `/opt/render/project/src/files/databse.db`
   - `ALLOWED_ORIGINS`: `*` (или ваш домен)
6. Нажмите **Create Web Service**
7. Готово! API будет доступен по адресу `https://your-app.onrender.com`

**Плюсы**: Бесплатно, автоматический SSL, простота
**Минусы**: Может "засыпать" после неактивности на бесплатном тарифе

---

## 🚀 Вариант 2: Railway (Рекомендуется)

1. Зарегистрируйтесь на https://railway.app
2. Нажмите **New Project** → **Deploy from GitHub repo**
3. Выберите ваш репозиторий
4. Railway автоматически определит Python проект
5. Добавьте переменные окружения в настройках:
   - `DATABASE_PATH`: `/app/files/databse.db`
   - `ALLOWED_ORIGINS`: `*`
6. Railway автоматически развернет проект
7. Получите домен: `https://your-app.up.railway.app`

**Плюсы**: Простота, автоматический SSL, хорошая производительность
**Минусы**: Ограниченный бесплатный тариф

---

## 🚀 Вариант 3: Fly.io (Бесплатный тариф)

1. Установите Fly CLI: `curl -L https://fly.io/install.sh | sh`
2. Зарегистрируйтесь: `fly auth signup`
3. В директории проекта: `fly launch`
4. Следуйте инструкциям
5. Развертывание: `fly deploy`

**Плюсы**: Бесплатно, хорошая производительность
**Минусы**: Требует установки CLI

---

## 🚀 Вариант 4: Heroku

1. Установите Heroku CLI: https://devcenter.heroku.com/articles/heroku-cli
2. Войдите: `heroku login`
3. Создайте приложение: `heroku create food2mood-api`
4. Настройте переменные:
   ```bash
   heroku config:set DATABASE_PATH=/app/files/databse.db
   heroku config:set ALLOWED_ORIGINS=*
   ```
5. Развертывание: `git push heroku main`

**Плюсы**: Популярная платформа, много документации
**Минусы**: Платный тариф (от $7/месяц)

---

## 🖥️ Вариант 5: VPS (DigitalOcean, Vultr и т.д.)

### Быстрая установка на Ubuntu:

```bash
# 1. Подключитесь к серверу
ssh root@your-server-ip

# 2. Установите зависимости
apt update && apt install -y python3 python3-pip python3-venv git nginx

# 3. Клонируйте проект
cd /opt
git clone https://github.com/yourusername/koreanchick.git
cd koreanchick

# 4. Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate
pip install -r api/requirements.txt

# 5. Установите gunicorn для продакшена
pip install gunicorn

# 6. Запустите через gunicorn
gunicorn api.main_prod:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000

# 7. Настройте Nginx (создайте /etc/nginx/sites-available/food2mood)
# См. полную инструкцию в DEPLOYMENT_GUIDE.md
```

---

## 📝 Чеклист перед развертыванием

- [ ] Проверить, что код работает локально
- [ ] Обновить `ALLOWED_ORIGINS` в переменных окружения
- [ ] Убедиться, что база данных доступна
- [ ] Проверить, что все зависимости в `requirements.txt`
- [ ] Протестировать API локально с `main_prod.py`

---

## 🔧 Настройка после развертывания

1. **Проверьте работу API**:
   ```bash
   curl https://your-api-url.com/health
   ```

2. **Откройте документацию**:
   - Swagger UI: `https://your-api-url.com/docs`
   - ReDoc: `https://your-api-url.com/redoc`

3. **Настройте CORS** (важно для безопасности):
   - Измените `ALLOWED_ORIGINS` на конкретные домены вашего фронтенда

---

## 🆘 Решение проблем

### API не запускается
- Проверьте логи на платформе
- Убедитесь, что все зависимости установлены
- Проверьте переменные окружения

### Ошибка подключения к БД
- Проверьте путь к базе данных
- Убедитесь, что файл БД существует
- Проверьте права доступа

### CORS ошибки
- Обновите `ALLOWED_ORIGINS` в переменных окружения
- Перезапустите приложение

---

**Выберите вариант, который вам подходит, и следуйте инструкциям! 🚀**

